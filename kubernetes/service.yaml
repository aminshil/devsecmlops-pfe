apiVersion: v1
kind: Service
metadata:
  name: anomaly-api
  namespace: ml-serving
spec:
  type: NodePort
  selector:
    app: anomaly-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
