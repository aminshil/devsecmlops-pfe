# Host (VM) environment: training, evaluation, monitoring agents, tooling.
#
# The serving image installs ONLY requirements-api.txt. It is included here
# first so that every library shared with the image (pandas, scikit-learn,
# xgboost, joblib, fastapi, psycopg, prometheus-client, ...) is pinned to the
# SAME version on the host that trains the models and in the container that
# serves them -- pickled models must be loaded by the version that wrote them.
-r requirements-api.txt

# Experiment tracking (ml-model/train_production.py logs runs here when
# MLFLOW_TRACKING_URI is set)
mlflow==3.16.1

# Evaluation / demo scripts
requests==2.33.1
rich==14.1.0

# Security floors for transitive dependencies (pip-audit findings):
#   starlette < 1.3.1  PYSEC-2026-161/248/249/2280/2281
#   pyasn1    < 0.6.4  PYSEC-2026-3455/3456/3457
starlette>=1.3.1
pyasn1>=0.6.4
cryptography>=50.0.0
